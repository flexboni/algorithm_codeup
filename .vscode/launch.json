{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.out",
            "args": [],
            "preLaunchTask": "g++ build active file",
            "stdio": [null, null, null],
            // "stdio": [stdin, stdout, stderr], // 파일 입출력 하고 싶다면 주석 해제
            "terminal": "integrated"
        }
    ]
}